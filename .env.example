# Postgres (Docker compose or Laragon)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=testclaudecode_app
POSTGRES_PORT=5432

# Used by Prisma in /server (.env there is the source of truth for the server)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/testclaudecode_app?schema=public
