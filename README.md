# HealthCare-App

Fullstack healthcare app — Express + Prisma + PostgreSQL (server), React 19 + Vite + Tailwind v4 + shadcn/ui (client).

## Quick start

```powershell
pnpm install
pnpm db:migrate
pnpm db:seed
pnpm dev
```

Server: http://localhost:3000 — Client: http://localhost:5173
